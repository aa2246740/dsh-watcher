# Cloud agent constraints

Only use grok 4.6.

Prefer Grok or Composer only.

Never Claude / Sonnet / Opus / GPT / Gemini.

Do not spawn Task / computerUse / browser unless explicitly required.

Subagents must not pass a model slug — inherit the parent.
