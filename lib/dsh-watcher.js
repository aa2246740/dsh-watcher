//#region lib/types/dsh-watcher.js
const name = "dsh-watcher";
const inject = [];
/** Host half only joins the Web plugin graph. Observation is the client fold. */
function apply(_ctx) {
	console.log("[my-plugins/dsh-watcher] loaded");
}
//#endregion
export { apply, inject, name };
